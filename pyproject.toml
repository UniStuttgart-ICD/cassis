[project]
name = "cassis-tests"
version = "1.3.2"
description = "Integration tests for the Cassis Grasshopper plugin"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.25.0",
    "rich>=13.0.0",
]
